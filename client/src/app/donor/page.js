import SearchDonor from '@/features/DonorSearch/SearchDonor';

export const metadata = {
  title: 'Find Donors | Clinical Vitality',
};

export default function SearchDonorRoute() {
  return <SearchDonor />;
}
