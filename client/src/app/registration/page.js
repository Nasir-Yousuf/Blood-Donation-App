import RegisterPage from '@/features/Registration/Register';

export const metadata = {
  title: 'Register | Clinical Vitality',
};

export default function RegistrationRoute() {
  return <RegisterPage />;
}
